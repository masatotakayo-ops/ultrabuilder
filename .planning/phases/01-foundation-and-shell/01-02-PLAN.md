---
phase: 01-foundation-and-shell
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/desktop/package.json
  - apps/desktop/tsconfig.json
  - apps/desktop/vite.config.ts
  - apps/desktop/index.html
  - apps/desktop/src/main.tsx
  - apps/desktop/src/App.tsx
  - apps/desktop/src/styles/globals.css
  - apps/desktop/src/components/layout/AppShell.tsx
  - apps/desktop/src/components/layout/Titlebar.tsx
  - apps/desktop/src/components/layout/ActivityBar.tsx
  - apps/desktop/src/components/layout/Sidebar.tsx
  - apps/desktop/src/components/layout/EditorArea.tsx
  - apps/desktop/src/components/layout/BottomPanel.tsx
  - apps/desktop/src/stores/ui-store.ts
  - apps/desktop/src/routes/__root.tsx
  - apps/desktop/src/routes/index.tsx
  - apps/desktop/src/routes/projects/index.tsx
  - apps/desktop/src-tauri/tauri.conf.json
  - apps/desktop/src-tauri/Cargo.toml
  - apps/desktop/src-tauri/src/main.rs
  - apps/desktop/src-tauri/src/lib.rs
  - apps/desktop/src-tauri/capabilities/default.json
autonomous: true
requirements:
  - FNDN-01

must_haves:
  truths:
    - "Tauri desktop app launches and shows the React UI with VS Code-style layout"
    - "Custom titlebar is draggable and shows app name"
    - "Activity bar, sidebar, editor area, and bottom panel are visible"
    - "Sidebar collapses and expands via toggle button"
    - "System theme (light/dark) is respected"
    - "Geist Sans and Geist Mono fonts render correctly"
    - "Native app menu has File, Edit, View, Window, Help submenus"
    - "TanStack Router navigates to /projects route"
  artifacts:
    - path: "apps/desktop/src/components/layout/AppShell.tsx"
      provides: "VS Code-style layout container"
      contains: "ActivityBar"
    - path: "apps/desktop/src/components/layout/Titlebar.tsx"
      provides: "Custom draggable titlebar"
      contains: "data-tauri-drag-region"
    - path: "apps/desktop/src-tauri/tauri.conf.json"
      provides: "Tauri configuration with decorations: false"
      contains: "decorations"
    - path: "apps/desktop/src-tauri/src/main.rs"
      provides: "App setup with menu and plugins"
      contains: "Menu"
    - path: "apps/desktop/src/routes/__root.tsx"
      provides: "Root route with AppShell layout"
      contains: "createRootRoute"
  key_links:
    - from: "apps/desktop/src/App.tsx"
      to: "apps/desktop/src/routes/__root.tsx"
      via: "TanStack Router provider"
      pattern: "RouterProvider"
    - from: "apps/desktop/src/stores/ui-store.ts"
      to: "apps/desktop/src/components/layout/AppShell.tsx"
      via: "Zustand useUiStore hook"
      pattern: "useUiStore"
    - from: "apps/desktop/src/styles/globals.css"
      to: "apps/desktop/src/main.tsx"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Create the Tauri desktop application with React UI, VS Code-style layout, custom titlebar, native app menu, TanStack Router, Zustand state, shadcn/ui + Tailwind 4, and Geist fonts.

Purpose: FNDN-01 requires the Tauri app to launch cross-platform with React UI. This plan delivers the permanent app shell that all future phases slot into (per D-01).
Output: A launchable Tauri desktop app with the IDE-like layout, routing, and theming.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-shell/01-CONTEXT.md
@.planning/phases/01-foundation-and-shell/01-RESEARCH.md
@.planning/phases/01-foundation-and-shell/01-01-SUMMARY.md
</context>

<tasks>

<task id="01-02-T1" type="auto" title="Scaffold Tauri app with React+TS+Vite and install all frontend dependencies">
  <read_first>
    - package.json (root monorepo config from plan 01)
    - pnpm-workspace.yaml (workspace globs)
    - .planning/phases/01-foundation-and-shell/01-RESEARCH.md (Installation section lines 117-161, Standard Stack table)
  </read_first>
  <files>apps/desktop/package.json, apps/desktop/tsconfig.json, apps/desktop/vite.config.ts, apps/desktop/index.html, apps/desktop/src/main.tsx, apps/desktop/src/styles/globals.css, apps/desktop/src-tauri/tauri.conf.json, apps/desktop/src-tauri/Cargo.toml, apps/desktop/src-tauri/capabilities/default.json</files>
  <action>
    1. Scaffold the Tauri app. Run from project root:
       ```
       pnpm create tauri-app apps/desktop --template react-ts --manager pnpm
       ```
       If create-tauri-app does not support the --template flag or output path cleanly, manually scaffold:
       - Create apps/desktop/ directory
       - Initialize with `pnpm init` inside apps/desktop
       - Install Tauri CLI: `pnpm add -D @tauri-apps/cli`
       - Run `pnpm tauri init` inside apps/desktop

    2. Install frontend dependencies in apps/desktop (exact versions from RESEARCH.md):
       ```
       pnpm add react@^19.2 react-dom@^19.2
       pnpm add @tauri-apps/api@^2.10 @tauri-apps/plugin-sql@^2.3 @tauri-apps/plugin-process@^2.3
       pnpm add tailwindcss@^4.2 @tailwindcss/vite@^4.2
       pnpm add zustand@^5.0
       pnpm add @tanstack/react-router@^1.168 @tanstack/react-router-devtools@^1.166
       pnpm add @tanstack/react-query@^5.95
       pnpm add drizzle-orm@^0.45
       pnpm add lucide-react@^1.7 geist@^1.7
       pnpm add -D @tanstack/router-plugin@^1.167
       pnpm add -D drizzle-kit@^0.31
       pnpm add -D @biomejs/biome@^2.4
       pnpm add -D vitest@^4.1 @testing-library/react
       pnpm add -D vite@^8.0 @vitejs/plugin-react
       pnpm add -D typescript@^6.0
       ```

    3. Add workspace dependency on @ultrabuilder/shared:
       ```
       pnpm add @ultrabuilder/shared@workspace:*
       ```

    4. Configure vite.config.ts (exact content from RESEARCH.md Pattern 6):
       ```typescript
       import { defineConfig } from "vite";
       import react from "@vitejs/plugin-react";
       import tailwindcss from "@tailwindcss/vite";
       import { TanStackRouterVite } from "@tanstack/router-plugin/vite";
       import path from "path";

       export default defineConfig({
         plugins: [
           TanStackRouterVite({ target: "react", autoCodeSplitting: true }),
           react(),
           tailwindcss(),
         ],
         resolve: {
           alias: {
             "@": path.resolve(__dirname, "./src"),
           },
         },
         server: {
           port: 1420,
           strictPort: true,
         },
         envPrefix: ["VITE_", "TAURI_"],
       });
       ```

    5. Configure apps/desktop/src/styles/globals.css (RESEARCH.md Code Examples: Geist Font Setup):
       ```css
       @import "tailwindcss";
       @import "geist/font/sans.css";
       @import "geist/font/mono.css";

       @theme {
         --font-sans: "Geist Sans", ui-sans-serif, system-ui, sans-serif;
         --font-mono: "Geist Mono", ui-monospace, monospace;
       }
       ```
       Per D-16: Geist Sans for UI text, Geist Mono for code/paths/timestamps.

    6. Configure tauri.conf.json per RESEARCH.md Pattern 4:
       - Set window: title "Ultrabuilder", width 1280, height 800, minWidth 900, minHeight 600
       - Set "decorations": false (per D-13)
       - Set transparent: false

    7. Configure Cargo.toml with Rust dependencies (from RESEARCH.md):
       ```toml
       [dependencies]
       tauri = { version = "2.10", features = ["tray-icon"] }
       tauri-plugin-sql = { version = "2.3", features = ["sqlite"] }
       tauri-plugin-log = "2.8"
       serde = { version = "1", features = ["derive"] }
       serde_json = "1"
       tokio = { version = "1", features = ["full"] }
       tracing = "0.1"
       tracing-subscriber = { version = "0.3", features = ["json"] }
       uuid = { version = "1", features = ["v4", "serde"] }

       [build-dependencies]
       tauri-build = { version = "2.10", features = [] }
       ```

    8. Configure capabilities/default.json (from RESEARCH.md Pattern 4):
       ```json
       {
         "identifier": "default",
         "windows": ["main"],
         "permissions": [
           "core:default",
           "core:window:default",
           "core:window:allow-start-dragging",
           "core:window:allow-minimize",
           "core:window:allow-maximize",
           "core:window:allow-unmaximize",
           "core:window:allow-close",
           "core:window:allow-set-title",
           "sql:default",
           "sql:allow-execute",
           "sql:allow-select",
           "process:default",
           "log:default"
         ]
       }
       ```

    9. Initialize shadcn/ui:
       ```
       npx shadcn@latest init
       ```
       Select: Tailwind CSS v4, React, TypeScript. This generates components.json and lib/utils.ts.

    10. Add shadcn components needed for the shell:
        ```
        npx shadcn@latest add button separator tooltip tabs scroll-area resizable
        ```
  </action>
  <acceptance_criteria>
    - apps/desktop/package.json exists and contains "react", "@tauri-apps/api", "zustand", "@tanstack/react-router", "drizzle-orm", "geist", "lucide-react"
    - apps/desktop/package.json contains "@ultrabuilder/shared": "workspace:*"
    - apps/desktop/vite.config.ts contains "TanStackRouterVite" and "@tailwindcss/vite" and port 1420
    - apps/desktop/src/styles/globals.css contains '@import "tailwindcss"' and '@import "geist/font/sans.css"'
    - apps/desktop/src-tauri/tauri.conf.json contains "decorations": false and "minWidth": 900
    - apps/desktop/src-tauri/Cargo.toml contains 'tauri = { version = "2.10"' and 'uuid' and 'tracing'
    - apps/desktop/src-tauri/capabilities/default.json contains "core:window:allow-start-dragging"
    - `pnpm install` exits 0 from project root
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/moatazbinali/Dropbox/7_Code/Ultrabuilder && test -f apps/desktop/vite.config.ts && test -f apps/desktop/src-tauri/Cargo.toml && grep -q "decorations" apps/desktop/src-tauri/tauri.conf.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Tauri app scaffolded with all frontend deps, Vite config, Tailwind 4, Geist fonts, TanStack Router plugin, and Rust crate dependencies</done>
</task>

<task id="01-02-T2" type="auto" title="Build VS Code-style layout shell with custom titlebar, router, Zustand state, and native menu">
  <read_first>
    - apps/desktop/src-tauri/tauri.conf.json (from T1)
    - apps/desktop/vite.config.ts (from T1)
    - apps/desktop/src/styles/globals.css (from T1)
    - .planning/phases/01-foundation-and-shell/01-RESEARCH.md (Patterns 4, 5, 6, 8 — Titlebar, Menu, Router, Zustand)
    - .planning/phases/01-foundation-and-shell/01-CONTEXT.md (D-01 through D-04, D-13 through D-16)
  </read_first>
  <files>apps/desktop/src/main.tsx, apps/desktop/src/App.tsx, apps/desktop/src/components/layout/AppShell.tsx, apps/desktop/src/components/layout/Titlebar.tsx, apps/desktop/src/components/layout/ActivityBar.tsx, apps/desktop/src/components/layout/Sidebar.tsx, apps/desktop/src/components/layout/EditorArea.tsx, apps/desktop/src/components/layout/BottomPanel.tsx, apps/desktop/src/stores/ui-store.ts, apps/desktop/src/routes/__root.tsx, apps/desktop/src/routes/index.tsx, apps/desktop/src/routes/projects/index.tsx, apps/desktop/src-tauri/src/main.rs, apps/desktop/src-tauri/src/lib.rs</files>
  <action>
    **React Entry Point (main.tsx):**
    - Import globals.css
    - Render App component into root div

    **App.tsx:**
    - Create TanStack Router instance with routeTree
    - Wrap with QueryClientProvider (TanStack Query)
    - Render RouterProvider
    - Per D-04: TanStack Router from day one

    **Zustand UI Store (stores/ui-store.ts):**
    - Exact implementation from RESEARCH.md Pattern 8
    - State: sidebarOpen (default true), bottomPanelOpen (default true), bottomPanelTab ("log" | "ipc-debug" | "problems"), theme ("light" | "dark" | "system"), appReady, windowFocused
    - Actions: toggleSidebar, toggleBottomPanel, setBottomPanelTab, setAppReady, setWindowFocused
    - Per D-02: sidebar collapsible from day one

    **AppShell.tsx (per D-01):**
    - VS Code-style layout: flex row with:
      - Titlebar (top, full width, h-9)
      - Below titlebar: flex row containing:
        - ActivityBar (far left, w-12, full height, flex-col, icons)
        - Sidebar (collapsible, w-60 when open, 0 when closed, transition)
        - Main area (flex-1, flex-col):
          - EditorArea (flex-1, contains Outlet for router)
          - BottomPanel (collapsible, h-48 when open)
    - Read sidebarOpen/bottomPanelOpen from useUiStore

    **Titlebar.tsx (per D-13):**
    - Use `data-tauri-drag-region` attribute on the container div
    - Show "Ultrabuilder" centered text
    - Import window controls from @tauri-apps/api/window for minimize/maximize/close
    - Detect platform: on macOS, leave space for traffic lights on left (w-[70px]); on Windows/Linux, show custom minimize/maximize/close buttons on right
    - Platform detection via navigator.userAgent or Tauri os plugin

    **ActivityBar.tsx:**
    - Vertical icon bar (w-12, bg-muted, border-r)
    - Icons from lucide-react: FolderKanban (projects), Settings (settings)
    - Tooltip on hover for each icon
    - Active state highlighting

    **Sidebar.tsx (per D-02):**
    - Collapsible via useUiStore.sidebarOpen
    - When open: w-60 with header "Explorer" and content area
    - Transition: width with duration-200
    - Phase 1 content: simple project navigation list (placeholder, real content in plan 03)

    **EditorArea.tsx (per D-03):**
    - Contains TanStack Router Outlet
    - Phase 1 renders /projects route in this area

    **BottomPanel.tsx:**
    - Collapsible via useUiStore.bottomPanelOpen
    - Tab bar with: Log | IPC Debug | Problems
    - Tab switching via useUiStore.setBottomPanelTab
    - Content area placeholder for each tab (real content in plans 03/04)
    - Per D-12: IPC Debug tab only shown when import.meta.env.DEV

    **Routes:**
    - __root.tsx: createRootRoute with AppShell as component
    - index.tsx: redirect to /projects
    - projects/index.tsx: placeholder Projects page with "Projects" heading (full content in plan 03)

    **Rust main.rs (per D-14, D-15):**
    - Build native app menu using Tauri Menu API (exact code from RESEARCH.md Pattern 5):
      - File: New Project (CmdOrCtrl+N), Open Project (CmdOrCtrl+O), separator, Quit
      - Edit: Undo, Redo, separator, Cut, Copy, Paste, Select All
      - View: Toggle Sidebar (CmdOrCtrl+B), Toggle Bottom Panel (CmdOrCtrl+J), separator, Zoom In, Zoom Out, Reset Zoom
      - Window: Minimize, Zoom, Close
      - Help: About Ultrabuilder
    - Register tauri-plugin-sql with "sqlite:ultrabuilder.db" migration path
    - Register tauri-plugin-log
    - Register tauri-plugin-process
    - Handle menu events: emit "menu:toggle-sidebar" and "menu:toggle-bottom-panel" as Tauri events so React can listen

    **Rust lib.rs:**
    - Placeholder invoke_handler with empty command list (commands added in plan 03)
    - Export run() function that builds and runs the app

    **Theme (per D-15):**
    - In globals.css, add dark mode styles using shadcn's "dark" class convention
    - In AppShell, detect OS theme preference via `window.matchMedia("(prefers-color-scheme: dark)")` and set/remove "dark" class on document.documentElement
    - Store theme preference in ui-store
  </action>
  <acceptance_criteria>
    - apps/desktop/src/components/layout/AppShell.tsx exists and contains "ActivityBar", "Sidebar", "EditorArea", "BottomPanel", "Titlebar"
    - apps/desktop/src/components/layout/Titlebar.tsx contains "data-tauri-drag-region" and "Ultrabuilder"
    - apps/desktop/src/components/layout/Sidebar.tsx contains "useUiStore" and "sidebarOpen"
    - apps/desktop/src/components/layout/BottomPanel.tsx contains "ipc-debug" and "import.meta.env"
    - apps/desktop/src/stores/ui-store.ts contains "toggleSidebar", "toggleBottomPanel", "sidebarOpen", "bottomPanelOpen"
    - apps/desktop/src/routes/__root.tsx contains "createRootRoute" and "AppShell"
    - apps/desktop/src/routes/projects/index.tsx exists
    - apps/desktop/src/App.tsx contains "RouterProvider" and "QueryClientProvider"
    - apps/desktop/src-tauri/src/main.rs contains "Menu" and "File" and "Edit" and "View" and "Window" and "Help"
    - apps/desktop/src-tauri/src/main.rs contains "toggle-sidebar" and "toggle-bottom-panel"
    - apps/desktop/src/styles/globals.css contains "Geist Sans" and "Geist Mono"
    - `cd apps/desktop && pnpm tauri build` exits 0 OR `cargo build` exits 0 in src-tauri
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/moatazbinali/Dropbox/7_Code/Ultrabuilder/apps/desktop && grep -q "data-tauri-drag-region" src/components/layout/Titlebar.tsx && grep -q "ActivityBar" src/components/layout/AppShell.tsx && grep -q "toggleSidebar" src/stores/ui-store.ts && grep -q "Menu" src-tauri/src/main.rs && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Tauri app launches with VS Code-style layout (D-01), custom titlebar (D-13), collapsible sidebar (D-02), full IDE menu (D-14), system-aware theme (D-15), Geist fonts (D-16), TanStack Router (D-04), and /projects route (D-03)</done>
</task>

</tasks>

<verification>
- `pnpm turbo build` from monorepo root exits 0 (packages/shared builds, then apps/desktop builds)
- Tauri app launches: `cd apps/desktop && pnpm tauri dev`
- VS Code-style layout visible: titlebar + activity bar + sidebar + editor area + bottom panel
- Sidebar toggles open/closed
- Bottom panel toggles open/closed
- System dark/light theme is respected
- Native menu bar shows File/Edit/View/Window/Help
- /projects route loads in editor area
</verification>

<success_criteria>
- FNDN-01 satisfied: Tauri desktop app launches with React UI
- D-01: VS Code layout with all panels present
- D-02: Sidebar collapsible
- D-03: Projects screen in main area (placeholder, full CRUD in plan 03)
- D-04: TanStack Router with /projects route
- D-13: Custom titlebar with drag region
- D-14: Full IDE menu (File/Edit/View/Window/Help)
- D-15: System-aware light/dark theme
- D-16: Geist fonts loaded and rendering
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-shell/01-02-SUMMARY.md`
</output>
